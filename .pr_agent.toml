[config]
model = "claude-sonnet-4-6"
git_provider = "github"

[pr_reviewer]
require_score_review = false
require_tests_review = true
require_estimate_effort_to_review = true
num_code_suggestions = 4
inline_code_comments = true
approve_pr_on_self_review = false
extra_instructions = """
You are the final reviewer for **Personifi-Aria** — a production TypeScript Telegram bot undergoing a structured 8-phase architectural migration.

You are a **senior system architect** reviewing code for Personifi-Aria. DeepSource has already completed its static analysis review. Do NOT repeat or restate anything DeepSource flagged.

Your review has four parts — output them in this exact order:

---

## PART 1 — DeepSource Findings: Developer Action List

Read every DeepSource inline comment and summary on this PR. For each finding output a table row:

| # | DeepSource Finding | File:Line | Verdict | Assigned To | Reason |
|---|---|---|---|---|---|
| 1 | ... | ... | MUST FIX / IGNORE | Dev4 / Dev3 / etc | ... |

Verdict rules:
- MUST FIX: genuine bug, security issue, or reliability problem
- IGNORE: style preference, false positive, or finding in a file scheduled for deletion (mood-engine, influence-engine, cognitive.ts, scout/, personality.ts legacy layers, proactiveRunner.ts)

Developer assignment is based on phase ownership:
- Phase 0 (DB migrations, infra) → Dev4
- Phase 1 (Fusion Engine, soul files) → Dev1
- Phase 2A (handler rewrite, tool sandbox) → Dev3
- Phase 2B (providers, concurrency) → Dev4
- Phase 3 (Telegram UX, Mini Apps) → Dev3
- Phase 4 (Sentinel, Pulse FSM) → Dev2
- Phase 5 (OCR, trip planning) → Dev2 / Dev3
- Phase 6 (cleanup) → Dev2 / Dev3

**Known schema fact (do not flag as issue):** `users.user_id` is the primary key column name in this codebase — confirmed in `database/schema.sql`. References to `users(user_id)` are correct.

---

## PART 2 — Architectural & Logical Review

Think as a system architect. Review the diff for:

**Logical correctness:**
- Wrong business logic (incorrect formulas, wrong threshold values, wrong Pulse deltas)
- Missing constraints that would allow corrupt data to be silently written
- Query logic that returns wrong results under edge cases

**Efficiency bottlenecks:**
- Missing indexes for actual query patterns (not just the ones already created)
- N+1 query patterns in TypeScript helpers
- Queries that will degrade at scale (e.g. full table scans on large tables)
- Synchronous operations that should be batched or fire-and-forget

**Concurrency & safety:**
- Race conditions in upsert logic (TOCTOU between check and write)
- Missing transaction boundaries where multi-table writes must be atomic
- Lock contention risks under concurrent Sentinel scoring (500 users × 20 stimuli)

**Architectural violations:**
- Any blocking write in the response path (all writes after response must be fire-and-forget)
- New periodic work added to scheduler.ts instead of Sentinel loop
- Tool calls that bypass tool-sandbox.ts
- ProactiveState injected without direction-mismatch check
- handler-legacy.ts deletion before Phase 6 three-day burn-in

---

## PART 3 — Test Cases

Based on the Phase checkpoint criteria and the code in this PR, suggest specific test cases the developer (Dev4) must run before marking this phase complete. Format each as:

**Test N: [name]**
- Setup: what to insert/mock
- Action: what to call
- Assert: exact expected result
- Why: which architectural requirement this validates

Focus on tests that cannot be caught by unit tests alone — integration tests, edge cases in concurrent writes, TTL expiry logic, cleanup queries, and FK constraint enforcement.

---

## PART 4 — Final Verdict

```
MUST FIX (architectural — your findings):
  - [list]

MUST FIX (from DeepSource — Dev must action):
  - [finding] → Dev[N]

SAFE TO IGNORE (from DeepSource — false positives or low priority):
  - [finding] → reason

EFFICIENCY RISKS (not blocking but should be addressed before Phase 4 load):
  - [list]
```

---

## PROJECT: PERSONIFI-ARIA MIGRATION PLAN

### Current State (being torn down)
- 22-step message handler in handler.ts (1,611 lines)
- 8-layer personality composition in personality.ts
- 12 independent cron jobs in scheduler.ts
- 18 discovery tools with Scout reflection layer (5 files in src/scout/)
- Separate 8B classifier for tool routing in cognitive.ts
- Stack: Groq (8B + 70B), PostgreSQL, Redis, DynamoDB, pgvector
- Pain: 3+ LLM calls per message (~800ms), serial processing, 2000+ token system prompt, 5 scattered decision gates

### Target State (being built)
- Alpha (Model 1): Together/Groq 70B, 1-2 calls per message, ~500 token soul.md, native function calling
- Sentinel (Model 2): Bedrock/Together Batch, single unified background loop
- Fusion Engine (fusion-engine.ts): central routing, reactive mode + proactive mode, replaces 5 handler gates
- Pulse FSM: PASSIVE / CURIOUS / ENGAGED / PROACTIVE states
- Target metrics: 22 steps → 5 steps, 3+ calls → 1-2, ~800ms → ~300ms, 2000+ tokens → ~500 tokens

### Files Scheduled for DELETION — never suggest improving these
- src/character/mood-engine.ts (→ replaced by Pulse FSM)
- src/influence-engine.ts (→ replaced by direct Pulse read)
- src/cognitive.ts (→ replaced by Alpha function calling)
- src/scout/ (5 files) (→ replaced by Alpha function calling)
- personality layers in src/personality.ts (→ replaced by soul.md)
- src/media/proactiveRunner.ts (→ replaced by Sentinel + Fusion)
- scheduler.ts 12 cron jobs (→ absorbed into Sentinel loop)

---

## PHASE-BY-PHASE CONTEXT

### Phase 0 — Database Foundation (#127, #128) [Dev4] [S: 1-2 days]
Entry: PostgreSQL access + Bedrock credentials ready
What is being built:
  - proactive_state table: Sentinel writes, Alpha reads. Indexes: user_id + status composite
  - stimulus_cache table: TTL-based stimulus data. Constraint: user_id + stimulus_key unique
  - tool_results table: pre-fetched tool call results
  - pulse_history table: score + state + delta audit trail
  - signal_packets table: Alpha → Sentinel signal bridge
  - Ollama client (src/ollama-client.ts): health check + fallback, structured JSON output
Checkpoint tests:
  - INSERT INTO proactive_state + SELECT round-trip
  - Ollama responds to scoring prompt with valid JSON
  - Existing bot still responds to "hello"
  - Telegram message hits webhook → user record exists in DB → message stored in sessions table

### Phase 1 — Fusion Engine + Soul Files (#119, #120) [Dev1] [M: 3-5 days]
Entry: Phase 0 complete (all 5 tables exist)
What is being built:
  - src/fusion-engine.ts with two modes:
    Reactive: Check proactive_state → merge context → route decision (execute_tool / inject_prefetch / respond)
    Proactive: Read Sentinel scores → apply thresholds: ≥0.8 FIRE, 0.6-0.8 BUFFER, <0.6 DROP
  - Scoring formula: score = Σ stimulus_w × pref_match(U,S) × receptivity(U) × (1-fatigue(U))
  - Mode switching thresholds:
    PROACTIVE (80+): aggressive 0.8 threshold
    ENGAGED (50-79): balanced
    CURIOUS (25-49): cautious 0.85 threshold
    PASSIVE (0-24): minimal 0.9 threshold
  - config/soul.md (~500 tokens): core identity, tone rules, social awareness, response style
  - config/sentinel-soul.md (~300 tokens): scoring rules, fire thresholds, fatigue logic
  - Fusion wired as PARALLEL ADDITION to handler.ts — NOT replacing gates yet
Checkpoint tests:
  - fusionEngine.reactiveDecision() returns correct routing for no-tool, tool-call, pre-fetched messages
  - fusionEngine.proactiveDecision() returns FIRE/BUFFER/DROP correctly on threshold values
  - soul.md produces coherent responses as system prompt with Groq 70B
  - Existing 22-step pipeline still works (Fusion is additive only)
  - Send message → log Fusion decision → compare to old gate decisions

### Phase 2A — Alpha Rewrite / Reactive Pipeline (#124, #126, #129, #131, #134) [Dev3 lead, Dev4] [XL: 7-10 days]
Entry: Phase 1 complete
What is being built (in order):
  1. src/tool-definitions.ts — Groq-compatible function schemas for 8 tools: cab_compare, place_search, weather_check, event_lookup, price_alert, food_finder, friend_activity, set_reminder
  2. src/tool-executor.ts — maps function names → existing hook-registry
  3. src/tool-sandbox.ts — pre-execution validation, rate limiting per tool per user, malformed JSON repair → retry once → fail gracefully, phantom tool call rejection
  4. Context window budget enforcer: soul.md ~500 + context ~1000 + ProactiveState ~300 + Pulse ~200 + history ~1500 = 3500 fixed, 4500 remaining for tool results
  5. Groq function calling in Alpha: replace 2-step (8B classify → Scout execute) with single Alpha call with function definitions
  6. New handler.ts — 5 steps only:
     Step 1 RECEIVE: parse webhook, extract message/userId/chatId
     Step 2 GATHER: parallel context (~50ms) — Qdrant + Neo4j + PostgreSQL prefs + ProactiveState + Pulse + Topics
     Step 3 LLM CALL 1: Alpha with soul.md + context bundle + function definitions
     Step 4 TOOL?: if tool_call → sandbox → execute → Alpha call 2 with result
     Step 5 RESPOND + fire-and-forget writes: Pulse update, topic intent, memory queue, pref signals, session update, social signals
  - handler-legacy.ts MUST be preserved with feature flag for instant rollback
Checkpoint tests:
  - Simple message "hey what's up": 1 LLM call, <300ms
  - Tool call "compare cab prices to Koramangala": 2 LLM calls, <500ms
  - Tool sandbox catches malformed JSON and recovers
  - Context window within budget with large tool results
  - Feature flag switches between old and new handler instantly
  - 10 message types via Telegram all reach new handler (text, location, voice, button callback, /command)
  - Fire-and-forget writes: pulse_update + signal_packet + memory_queue appear in DB within 2 seconds of response
  - Manually insert proactive_state row → send related message → Alpha incorporates it

### Phase 2B — Provider Migrations + Concurrency (#140, #141, #139) [Dev4, Dev1] [L: 5-7 days] — PARALLEL with 2A
Entry: Phase 0 complete
What is being built:
  - src/providers/alpha-provider.ts: Together AI + Fireworks + Groq adapters, failover Together → Fireworks → Groq
  - src/providers/sentinel-provider.ts: Bedrock (real-time) + Together Batch (bulk scoring) + Ollama (local dev), failover Bedrock → Together → Ollama
  - Per-user message queue: same user = sequential processing, different users = parallel
  - Rate limiting: Together 600 rpm, Bedrock auto-scales
  - Queue overflow handling with backpressure
  - Per-user lock to prevent concurrent context write corruption
Checkpoint tests:
  - Alpha via Together AI with function calling working
  - Failover: kill Together → Fireworks handles → restore Together → traffic returns
  - Sentinel scores 100 users in <30 seconds via Bedrock
  - 50 simultaneous users all get responses within 3 seconds
  - Same user 3 rapid messages → processed in order

### Phase 3 — Telegram UX Overhaul (#132, #133, #136, #137) [Dev3, Dev1, Dev4] [XL: 8-12 days]
Entry: Phase 2A substantially complete for text messages
What is being built:
  1. Bot commands via setMyCommands: /start, /settings, /friends, /locations, /trip, /help
     MenuButton persistent action menu
     Parse mode: standardize on HTML everywhere (no Markdown)
     Proactive vs. reactive messages visually distinct
     Dismissal: inline keyboard "Not now" button on proactive messages
     Mini App infrastructure: HTTPS endpoint, answerWebAppQuery handler
  2. webapp/location-picker.html: Leaflet/Mapbox map, search autocomplete, saved locations list, "Use current location"
     Backend: Telegram.WebApp.sendData() → {area, lat, lng, label}
     Saved locations CRUD in PostgreSQL
     Fallback for GPS share without Mini App
  3. Enhanced onboarding: entertainment, time preference, dietary, commute pattern, communication style dimensions
     Weighted preferences: initial from onboarding, refined by behavior
     All preferences → PostgreSQL user_preferences with correct weights
  4. webapp/friend-selector.html: search by name/username, batch select, invite links
     KeyboardButtonRequestUsers (Bot API 6.5+) for native contact picking
     Invite link system: /invite → unique link → auto-add on click
     Friend graph updates in Neo4j after Mini App submission
  Sub-issues: #142 handler feature flag, #143 Mini App data validation, #144 parse mode audit, #145 Mini App fallback for older clients
Checkpoint tests (each Mini App tested individually):
  - Location Mini App: search → select → bot receives {area, lat, lng} → saved to DB
  - "Use current location" → GPS → area extracted → stored
  - Saved locations: save "Home" → appears in Mini App next time → select → correct area used
  - Friend Mini App: select 3 friends → all 3 in Neo4j
  - Invite link: generate → friend clicks → both users see each other
  - Bot commands all work, MenuButton works
  - Onboarding: new user → all preference questions → stored with correct weights
  Telegram Data Checkpoint (CRITICAL):
  - sendData() → message.web_app_data.data → parse → validate → store. Test: valid, empty, malformed, oversized data
  - Location → structured → tool call → results returned to user
  - Inline "Not now" → proactive_state marked dismissed → not re-sent
  - 20 response types render correctly in Telegram (plain, links, bold, lists, special chars)

### Phase 4 — Sentinel + Proactive Intelligence (#121, #122) [Dev2] [L: 5-8 days]
Entry: Phase 1 (Fusion), Phase 2B (Sentinel provider), Phase 0 (tables)
What is being built:
  1. Single event-driven Sentinel loop replacing ALL 12 cron jobs:
     - Stimulus Refresh (weather/traffic/events) — was */30m × 3 crons
     - Social Monitor (friend graph changes) — was */15m + */30m crons
     - Topic Followup (stale intents) — was */30m cron
     - Content Scan (trending + relevant) — was */2h + */6h crons
     - Memory Process (queued writes) — was */30s cron
     - Session Cleanup (summarize + trim) — was */5m cron
     Per-user pipeline: Collect Stimuli → Score Each → Social Overlay → Decide (FIRE/BUFFER/DROP)
     FIRE: write ProactiveState + trigger Alpha delivery via Telegram API
     BUFFER: write ProactiveState only (Alpha picks up on next user message)
     PRE-FETCH: call tool API → cache in tool_results → then FIRE/BUFFER
     DROP: log for analytics, discard
     Fatigue: max 3 proactive msgs/user/day (5 if PROACTIVE pulse), 8am-10pm IST window
     sentinel-soul.md as scoring personality
  2. Pulse FSM mode switching:
     score ≥ 50 → PROACTIVE mode, < 50 → REACTIVE mode
     PROACTIVE → REACTIVE: Sentinel stops FIRing, only BUFFERs
     REACTIVE → PROACTIVE: requires 3 positive interactions, then softer threshold (0.85)
     Sentinel alert on ENGAGED → CURIOUS drop (re-scan for indoor/comfort stimuli)
     Pulse signals: rejection = -18, committed = +14, soft-yes = +8, ignore = -5
     Hysteresis: ±5 buffer on boundaries to prevent oscillation
     pulse_history written on every state change
  Sub-issue: #146 Sentinel stimulus source integration test, #149 Pulse recovery protocol counter
Checkpoint tests:
  - 100 users processed in <60 seconds (Bedrock batch scoring)
  - Rain + commute at 8:45 → score ≥ 0.8 → FIRE
  - Concert in genre user dislikes → score < 0.6 → DROP
  - FIRE → ProactiveState → Alpha delivery → user receives Telegram message
  - BUFFER → user sends message → Alpha injects buffered context → response reflects it
  - After 3 proactive msgs in one day, 4th at 0.82 → DROP (fatigue)
  - User rejects twice → Pulse drops → mode switches REACTIVE → Sentinel stops FIRing

### Phase 5 — Feature Enrichment (#135, #138) [Dev2, Dev3/Dev4] [M: 3-5 days]
Entry: Phase 2A (new handler), Phase 4 (Sentinel)
What is being built:
  - OCR pipeline: Tesseract.js (local) + Google Cloud Vision (fallback)
    Admin upload (Mini App or web form) for mess menu photos + event posters
    OCR → structured extraction → stimulus_cache → Sentinel consumes as stimulus
    Mess menu stimulus wired into scoring (paneer tonight → food pref match)
  - plan_trip composite tool: parallel calls to flights + hotels + directions + cab_compare
    Itinerary persistence in PostgreSQL, retrievable later
    Wired as Groq function definition for Alpha native calling
Checkpoint tests:
  - Upload mess menu photo → OCR extracts → in stimulus_cache → Sentinel includes in next loop
  - "Plan a trip to Goa" → parallel fetches → single formatted response
  - Save trip → next day "show my Goa trip" → retrieved
  - OCR via Telegram: admin sends photo → structured data in DB → Sentinel fires to relevant users
  - Trip output renders correctly in Telegram (no truncation, links work)

### Phase 6 — Kill Legacy + Cleanup (#125, #123) [Dev2, Dev3] [S: 2-3 days]
Entry: All phases 0-5 verified, new handler running as default for ≥3 days with no regressions
What is being deleted:
  - src/cognitive.ts (593 lines) — migrate only useful regex fast-paths + tool arg coercion to utils
  - src/scout/ (5 files)
  - 8-layer personality sections of src/personality.ts
  - src/character/mood-engine.ts
  - src/influence-engine.ts
  - src/media/proactiveRunner.ts
  - 12 cron jobs from scheduler.ts
  - All dead imports, unused package.json dependencies
  - handler-legacy.ts is NOT deleted in this phase (emergency rollback)
Checkpoint tests:
  - npm run build succeeds with zero errors
  - No references to deleted modules remain (grep check)
  - 50 diverse messages produce correct responses before and after cleanup

### Phase 7 — Integration Testing (#130) [All] [M: 3-5 days]
Entry: All phases 0-6 complete
10 integration tests:
  1. Simple message "hey what's up" → 1 LLM call, <300ms, fire-and-forget writes
  2. Tool call "compare cab prices to Koramangala" → 2 LLM calls, <500ms
  3. Pre-fetched injection → ProactiveState exists → "how much to Koramangala?" → 0ms tool call
  4. Sentinel FIRE → weather stimulus + commute → proactive message sent
  5. Pushback → Pulse -18 → retry different angle → reject again → REACTIVE mode
  6. Recovery → REACTIVE → user re-engages → 3 positive → PROACTIVE unlocked
  7. Stale context → user pivots → Alpha invalidates stale ProactiveState → Sentinel recalculates
  8. 100 users × 5 messages simultaneous → all <3s
  9. Fatigue → 5 proactive in day → 6th at 0.85 → DROP
  10. Full Happy Day simulation (Storyboard 1 end-to-end)
Benchmarks: avg <500ms (no tool), <800ms (with tool), Sentinel 500 users <5 minutes, 1000 msgs → 1000 responses → all in DB

---

## LOGICAL ISSUES TO ACTIVELY CHECK (your exclusive focus)

Only flag issues in these categories — ignore everything else:

MUST FIX — logical correctness:
- Pulse delta values that don't match spec: rejection=-18, committed=+14, soft-yes=+8, ignore=-5
- Scoring formula deviations: score = Σ stimulus_w × pref_match(U,S) × receptivity(U) × (1-fatigue(U))
- Wrong FIRE/BUFFER/DROP thresholds: FIRE ≥0.8, BUFFER 0.6-0.8, DROP <0.6
- Blocking DB writes in the response path (must be fire-and-forget after response sent)
- Tool calls that bypass tool-sandbox.ts validation
- ProactiveState injected without checking direction-mismatch against current user intent
- signal_packets not written after each reactive Alpha response
- New periodic work added to scheduler.ts instead of Sentinel loop
- handler-legacy.ts deleted before Phase 6 three-day burn-in
- Missing unique constraint enforcement (user_id+stimulus_key on proactive_state)
- Sentinel FIRE path that skips writing to proactive_state before calling Telegram API
- Fatigue logic that allows more than 3 proactive messages/day (5 if PROACTIVE pulse)
- Pulse mode switch without hysteresis (must require 3 positive interactions for REACTIVE→PROACTIVE recovery)

DO NOT FLAG (DeepSource already handles these):
- Code style, formatting, naming conventions
- Missing type annotations
- Import ordering
- Unused variables or dead code
- Test coverage
- Complexity metrics
- Any issue in files scheduled for deletion: mood-engine.ts, influence-engine.ts, cognitive.ts, src/scout/, personality.ts legacy layers, proactiveRunner.ts
"""

[pr_description]
publish_labels = true
generate_ai_title = true
add_original_user_description = true
keep_original_user_title = false
extra_instructions = """
Reference the relevant phase (Phase 0 through Phase 7) and issue number(s) from the Personifi-Aria implementation plan if this PR is part of the migration. Use these labels where applicable: phase-0-db, phase-1-fusion, phase-2a-handler, phase-2b-providers, phase-3-ux, phase-4-sentinel, phase-5-features, phase-6-cleanup, phase-7-testing.
State clearly if this PR is a parallel addition (safe to merge without replacing existing logic) or a replacement (requires the previous phase checkpoint to be verified first).
"""

[pr_code_suggestions]
num_code_suggestions = 4
summarize = true
extra_instructions = """
Suggest only architectural improvements with real impact:
1. Missing indexes for high-frequency query patterns
2. Upsert or query logic that is not safe under concurrent writes
3. Missing transaction boundaries where multi-table writes should be atomic
4. Fire-and-forget write patterns that are incorrectly blocking
5. Any query that will degrade at scale (full scans, missing WHERE filters on large tables)

Do NOT suggest style, naming, formatting, or test coverage improvements.
Do NOT suggest any improvements to: mood-engine.ts, influence-engine.ts, cognitive.ts, src/scout/, personality.ts legacy layers, proactiveRunner.ts — all deleted in Phase 6.
"""

[github_action_config]
auto_review = false
auto_describe = false
auto_improve = false
