/**
 * OCR module — Phase 5, Issue #135
 *
 * Public entry point for the OCR engine.
 */

export { parseMenuPhoto, parseEventPhoto } from './menu-parser.js'
export type { MenuItem, LocalEventData, OCRResult } from './types.js'
